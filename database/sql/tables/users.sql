CREATE TABLE users
(
    id            INT PRIMARY KEY AUTO_INCREMENT,
    last_name     VARCHAR(50),
    first_name    VARCHAR(50),
    middle_name   VARCHAR(50),
    date_of_birth DATE,
    gender        VARCHAR(10)
);



